export default {
  // Theme configuration
  theme: "./",
  // Default color schema
  colorSchema: "dark",
  // Configure server options with CORS
  server: {
    host: "0.0.0.0", // 允许外部访问
    port: 3030,
    cors: true, // 启用 CORS
  },
};
